package main

// Deprecated: seed.go was merged into scripts/tool.go (use `go run scripts/tool.go seed`).
// This package placeholder prevents the presence of another `package main` with
// a `main` function in the scripts directory. The implementation now lives in
// `scripts/tool.go`.
